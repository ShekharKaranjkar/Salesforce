@isTest

public class VehicleDocumentsSchedulerTestclass {
   /* Test.startTest();
    batchclass_vehicledocument sh1 = new batchclass_vehicledocument();
    String sch = '0 0 2 * * ?'; 
    system.schedule('Test Territory Check', sch, sh1); 
    Test.stopTest();  */
    // add system asserts to check your expected behaviour
}